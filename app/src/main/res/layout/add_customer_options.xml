<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/textView"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:text="Select existing or add brand new customer: " />

        <Spinner
            android:id="@+id/spinerPickCustomerToAdd"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="30dp"
            android:tooltipText="Select an existing customer here">

        </Spinner>

        <Button
            android:id="@+id/buttonAddSelected"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Add selected customer"
            android:layout_marginTop="30dp"/>

        <Button
            android:id="@+id/buttonCreateNewCustomerAcc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Add new customer" />
    </LinearLayout>

</android.support.constraint.ConstraintLayout>